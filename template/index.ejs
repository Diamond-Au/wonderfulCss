<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      :root {
        /* --blue: #007bff;
        --indigo: #6610f2;
        --purple: #6f42c1;
        --pink: #e83e8c;
        --red: #dc3545;
        --orange: #fd7e14;
        --yellow: #ffc107; */
        --themecolor: #28a745;
        --white: #fff;
        /* --teal: #20c997;
        --cyan: #17a2b8;
        --white: #fff;
        --gray: #6c757d;
        --gray-dark: #343a40;
        --primary: #007bff;
        --secondary: #6c757d;
        --success: #28a745;
        --info: #17a2b8;
        --warning: #ffc107;
        --danger: #dc3545;
        --light: #f8f9fa;
        --dark: #343a40; */
      }
      html,
      body {
        height: 100%;
      }
      a {
        text-decoration: none;
      }
      li {
        list-style: none;
      }
      body {
        font-family: "Open Sans", "Helvetica Neue", Helvetica, Arial, sans-serif;
        font-size: 14px;
        color: #646464;
        background-color: #f5f5f5;
      }
      .header {
        position: relative;
        text-align: center;
        background-color: var(--themecolor);
        color: #fff;
        margin-bottom: 0;
        height: 180px;
      }
      .header .title {
        margin: 0;
        font-family: "Source Sans Pro", "Open Sans", "Helvetica Neue", Helvetica,
          Arial, sans-serif;
        font-weight: 600;
        line-height: 180px;
        color: inherit;
        text-shadow: -5px 5px 0 rgba(0, 0, 0, 0.1);
      }
      .router-container {
        width: 70%;
        margin: 30px auto 0;
      }
      .router-container .router-list {
        display: flex;
        flex-wrap: wrap;
        justify-content: flex-start;
        align-items: flex-start;
      }
      .router-container .router-list .router-item {
        position: relative;
        background-color: var(--white);
        width: 32%;
        height: 80px;
        text-align: center;
        line-height: 80px;
        margin-right: 10px;
        margin-bottom: 20px;
      }
      .router-container .router-list .router-item::after {
        position: absolute;
        content: "";
        width: 35%;
        height: 0;
        z-index: 100;
        border-left: 20px solid transparent;
        border-top: 20px solid var(--themecolor);
        overflow: hidden;
        right: 0;
      }
      .router-container .router-list .router-item .index {
        position: absolute;
        top: 20px;
        color: var(--white);
        z-index: 150;
        right: 10%;
        top: -40%;
      }
      .router-container .router-list .router-item .router {
        display: inline-block;
        transform: translateX(-30px);
        font-size: 30px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1 class="title">导航界面</h1>
    </div>
    <div class="router-container">
      <ul class="router-list">
        <% routes.forEach((router, index) => { %>
          <li class="router-item">
            <span class="index">页面 <%= index%></span>
            <a class="router" href="<%= router.link + '/' + router.fileName %>"
            </li>
          <%= router.linkName%></a
        >
        <% }) %>
      </ul>
    </div>
  </body>
</html>
