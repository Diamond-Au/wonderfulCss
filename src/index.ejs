<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div class="router-container">
      <ul>
        <% routes.forEach((router) => { %>
        <a href="<%= router.link + '/' + router.fileName %>"
          ><%= router.linkName%></a
        >
        <% }) %>
      </ul>
    </div>
  </body>
</html>
